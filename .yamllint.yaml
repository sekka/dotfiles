ignore: |
  .github/workflows/

rules:
  line-length:
    max: 100
  comments:
    min-spaces-from-content: 1
  document-start: disable
  truthy:
    allowed-values: ["true", "false"]
    check-keys: false
