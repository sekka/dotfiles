#!/bin/bash

# Git pre-commit hook
# mise tasks でlint/formatを実行し、全て成功した場合のみcommitを許可

# dotfilesディレクトリでのチェック
SCRIPT="scripts/git/pre-commit-check.sh"
if [ -f "$SCRIPT" ]; then
  "./$SCRIPT"
else
  echo "⚠️  $SCRIPT が見つかりません"
  exit 1
fi
