# ============================================
# mise Configuration
# ============================================
# miseは開発環境管理ツールです
# Node.js、Python、Ruby等のバージョン管理とタスクランナー機能を提供します
#
# 使用方法:
# mise run <task>        # タスクを実行
# mise run <alias>       # エイリアスでタスクを実行
# mise list              # インストール済みツール一覧
# mise install           # .mise.tomlの設定に基づいてツールをインストール

# ============================================
# Environment Variables (環境変数設定)
# ============================================
# プロジェクト全体で使用される環境変数を定義
[env]
# Homebrew のバイナリを PATH に追加（node-gyp 等のネイティブビルドツール用）
_.path = ["/opt/homebrew/bin"]
# sharp がグローバルの libvips を無視し、prebuilt バイナリを使用するよう強制
SHARP_IGNORE_GLOBAL_LIBVIPS = "true"

# ============================================
# Global Settings (グローバル設定)
# ============================================
[settings]
# 実験的機能を有効化（新機能を試したい場合はtrue）
experimental = true
# リモートバージョン情報のキャッシュ期間
fetch_remote_versions_cache = "1h"
# リモートバージョン取得時のタイムアウト
fetch_remote_versions_timeout = "10s"
# 並列実行ジョブ数（CPUコア数に応じて調整）
jobs = 4
# レガシーバージョンファイル（.nvmrc、.ruby-version等）をサポート
legacy_version_file = true
# プラグイン自動更新チェックの間隔
plugin_autoupdate_last_check_duration = "7 days"
# 信頼するプロジェクトディレクトリ（これらのディレクトリ内の.mise.tomlは自動的に信頼される）
trusted_config_paths = ["~/dotfiles", "~/src/**", "~/projects/**"]
# Node.js ダウンロードの GPG 署名検証を無効化
# HTTPS によるダウンロード整合性で十分、GPG keyboxd の問題を回避
node.verify = false

# --------------------------------------
# GitHub/開発ツール
# --------------------------------------
[tasks.browserslist]
alias = "bl"
description = "Browserslist更新"
run = "npx browserslist@latest --update-db"

[tasks.gh-list-deploy]
description = "デプロイの実行履歴を表示する"
run = """
gh run list \
    --repo firstcontributions/first-contributions \
    --limit 10
"""

# --------------------------------------
# Git操作
# --------------------------------------
# Git log表示（シンプル）
[tasks.git-log-glog]
description = "シンプルなワンライン表示"
run = """
git log \
    --graph \
    --date=short \
    --pretty='format:%C(yellow)%h%C(reset) %C(green)%ad%C(reset) %C(blue)%an%C(reset)%C(red)%d%C(reset) %s'
"""

# Git log表示（詳細）
[tasks.git-log-glogd]
description = "詳細表示（相対時間、全ブランチ）"
run = """
git log \
    --graph \
    --all \
    --date=relative \
    --pretty='format:%C(yellow)%h%C(reset) %C(green)(%ar)%C(reset) %C(blue)%an%C(reset)%C(red)%d%C(reset) %s'
"""

# Git log表示（統計付き）
[tasks.git-log-glogs]
description = "統計情報付き詳細表示（変更ファイル数表示）"
run = """
git log \
    --graph \
    --stat \
    --date=iso \
    --pretty='format:%C(yellow)commit %H%C(reset)%C(red)%d%C(reset)%nAuthor: %C(blue)%an <%ae>%C(reset)%nDate:   %C(green)%ad%C(reset)%n%n    %s%n'
"""

# コードレビューツール
[tasks.git-util-difit-browser]
description = "コードレビューを開く"
run = "difit"

[tasks.git-util-difit-tui]
description = "コードレビューをTUIで開く"
run = "difit --tui"

[tasks.git-util-difit-here]
description = "カレントリポジトリのGit差分をdifitで表示"
run = "difit-here.sh"

[tasks.git-util-hbr]
description = "リポジトリをブラウザで開く"
run = "hub browse $(ghq list | fzf | cut -d '/' -f 2,3)"

# Claude Code関係のファイルを一覧表示
[tasks.llm-claude-ccexp]
description = "Claude Code関係のファイルを一覧表示"
run = "ccexp"

# --------------------------------------
# LLM/AI関連
# --------------------------------------
# Claude Code再開ツール
[tasks.llm-claude-ccresume]
description = "Claude Codeのclaude --resumeを使いやすくする"
run = "ccresume"

# Claude Codeセッション検索
[tasks.llm-claude-ccsearch]
description = "Claude Codeのセッション検索・共有"
run = "ccsearch"

# Claude Code使用状況
[tasks.llm-claude-ccusage]
description = "Claude Codeのセッション使用状況を表示"
run = "ccusage"

# RTK トークン節約量レポート
[tasks.llm-rtk-gain]
description = "RTKのトークン節約量をグラフ付きで表示"
run = "rtk gain --graph --quota"

# home/.claude/ のグローバル設定ファイル（シンボリックリンク元）
[tasks."llm-claude:sort-permissions-home-global"]
description = "home/.claude/settings.json の permissions を辞書順ソート"
run = "bash $HOME/dotfiles/scripts/development/sort-permissions.sh --file=$HOME/dotfiles/home/.claude/settings.json"

[tasks."llm-claude:sort-permissions-home-local"]
description = "home/.claude/settings.local.json の permissions を辞書順ソート"
run = "bash $HOME/dotfiles/scripts/development/sort-permissions.sh --file=$HOME/dotfiles/home/.claude/settings.local.json"

# dotfilesプロジェクト自体の設定ファイル
[tasks."llm-claude:sort-permissions-project-local"]
description = "dotfiles/.claude/settings.local.json の permissions を辞書順ソート"
run = "bash $HOME/dotfiles/scripts/development/sort-permissions.sh --file=$HOME/dotfiles/.claude/settings.local.json"

# codemass
[tasks.llm-codemass]
description = "codemassを起動"
run = "codemass"

# cc-sdd
[tasks.ccsdd-install]
description = "cc-sddを導入"
run = "npx cc-sdd@latest --lang ja --kiro-dir docs"

# --------------------------------------
# mise自体の管理
# --------------------------------------
# mise診断
[tasks.mise-doctor]
alias = "md"
description = "mise環境診断"
run = "mise doctor"

# mise list
[tasks.mise-list]
alias = "ml"
description = "インストール済みツール一覧"
run = "mise list"

# mise更新（Homebrew経由）
[tasks.mise-self-update]
alias = "msu"
description = "mise本体更新（Homebrew経由）"
run = "brew upgrade mise"

# mise upgrade
[tasks.mise-upgrade]
alias = "mu"
description = "miseのアップグレード（ツールのインストール/更新を実行）"
run = "mise upgrade"

# mise prune
[tasks.mise-prune]
alias = "mp"
description = "使われていないツールバージョンを削除"
run = "mise prune"

# mise キャッシュクリーンアップ
[tasks.mise-clean]
alias = "mc"
description = "miseのキャッシュとダウンロードをクリーンアップ"
run = """
echo "🧹 miseキャッシュをクリーンアップ中..."
rm -rf ~/.cache/mise/
rm -rf ~/.local/share/mise/downloads/*
mise prune -y
echo "✅ クリーンアップ完了"
"""

# 週次メンテナンス
[tasks.mise-weekly-clean]
description = "週次メンテナンス: キャッシュクリア + prune + doctor"
run = """
mise run mise-clean
mise doctor
"""

# --------------------------------------
# Rust関連
# --------------------------------------
# Cargo更新
[tasks.rust-cargo-update]
description = "cargo update"
run = "cargo install-update -a"

# プロセス監視
[tasks.rust-procs]
description = "プロセスモニタリング"
run = "procs"

# システム監視
[tasks.rust-rtop]
description = "システムモニタリング"
run = "rtop"

# --------------------------------------
# パフォーマンス測定
# --------------------------------------
# Lighthouseでサイト全体をスキャン
[tasks.perf-unlighthouse]
alias = "ulh"
description = "サイト全体のLighthouseスキャン（Unlighthouse）"
run = """
echo "使用方法: npx unlighthouse --site <URL>"
echo "例: npx unlighthouse --site https://example.com"
"""

# Lighthouseでサイト全体をスキャン（CI用）
[tasks.perf-unlighthouse-ci]
description = "CI環境でUnlighthouseを実行"
run = """
if [ -z "$UNLIGHTHOUSE_SITE" ]; then
  echo "❌ Error: UNLIGHTHOUSE_SITE 環境変数を設定してください"
  exit 1
fi
npx unlighthouse --site "$UNLIGHTHOUSE_SITE" --build-static
"""

# --------------------------------------
# Lint & Format (統合スクリプト経由)
# --------------------------------------
# 全ファイルチェック
[tasks.lint]
alias = "l"
description = "全ファイルのlintを実行"
run = """
bun ~/dotfiles/scripts/development/lint-format.ts --mode=check --verbose
"""

# 全ファイルフォーマット
[tasks.format]
alias = "f"
description = "全ファイルのformatを実行"
run = """
bun ~/dotfiles/scripts/development/lint-format.ts --mode=fix --verbose
"""

# ステージされたファイルをチェック
[tasks.lint-staged]
alias = "ls"
description = "ステージされたファイルのlintを実行"
run = """
bun ~/dotfiles/scripts/development/lint-format.ts --mode=check --staged --verbose
"""

# ステージされたファイルをフォーマット
[tasks.format-staged]
alias = "fs"
description = "ステージされたファイルのformatを実行"
run = """
bun ~/dotfiles/scripts/development/lint-format.ts --mode=fix --staged --verbose
"""

# Lintツールのセットアップ
[tasks.lint-setup]
description = "lintツールをセットアップ"
run = """
pre-commit install
echo "✅ Setup complete!"
"""

# --------------------------------------
# セットアップ
# --------------------------------------
# homeシンボリックリンク作成
[tasks.setup-home-symlink]
alias = "sds"
description = "homeのシンボリックリンクを作成"
run = "bash ~/dotfiles/setup/04_symlinks.sh"

# --------------------------------------
# Docker/Container
# --------------------------------------
# Colima起動
[tasks.docker-colima-start]
alias = "cs"
description = "Colimaを起動（Docker Desktop代替）"
run = "colima start"

# Colima停止
[tasks.docker-colima-stop]
alias = "cx"
description = "Colimaを停止"
run = "colima stop"

# Colima状態確認
[tasks.docker-colima-status]
description = "Colimaの状態を確認"
run = "colima status"

# --------------------------------------
# システム管理
# --------------------------------------
# スリープ防止（手動終了するまで）
[tasks.system-caffeinate]
alias = "cafe"
description = "スリープを防止（Ctrl+Cで終了）"
run = "caffeinate -dis"

# スリープ防止（1時間）
[tasks.system-caffeinate-1h]
alias = "cafe1"
description = "1時間スリープを防止"
run = "caffeinate -t 3600"

# スリープ防止（2時間）
[tasks.system-caffeinate-2h]
alias = "cafe2"
description = "2時間スリープを防止"
run = "caffeinate -t 7200"

# スリープ防止（4時間）
[tasks.system-caffeinate-4h]
alias = "cafe4"
description = "4時間スリープを防止"
run = "caffeinate -t 14400"

# ディスプレイスリープのみ防止
[tasks.system-caffeinate-display]
alias = "cafed"
description = "ディスプレイスリープのみ防止（Ctrl+Cで終了）"
run = "caffeinate -d"

# PATH表示
[tasks.system-envpath]
description = "envpath"
run = "echo $PATH | sed 's/:/\\n/g'"

# 環境変数表示
[tasks.system-envsort]
description = "環境変数をアルファベット順でソートして表示"
run = "env | sort"

# シェル再起動
[tasks.system-shreboot]
description = "SHELL再起動"
run = "exec $SHELL -l"

# Ghostty設定再読み込み
[tasks.system-ghostty-reload]
alias = "gr"
description = "Ghostty設定を再読み込み"
run = "echo '💡 Cmd + Shift + , を押してGhosttyの設定を再読み込みしてください'"

# tmux設定再読み込み
[tasks.system-tmux-reload]
description = "tmux設定を再読み込み"
run = "tmux source-file ~/.tmux.conf && tmux display-message 'tmux設定を再読み込みしました'"

# zsh設定再読み込み
[tasks.system-zsh-reload]
description = "zshの設定を再読み込み"
shell = "zsh -c"
run = "source ~/.zshrc && echo '✅ zsh設定を再読み込みしました'"

# --------------------------------------
# ユーティリティ
# --------------------------------------
# Homebrew更新
[tasks.update-brew]
alias = "ub"
description = "Homebrew更新"
run = "bash ~/dotfiles/setup/02_brew.sh"

# Homebrew明示的インストールパッケージ一覧
[tasks.brew-leaves]
alias = "blv"
description = "明示的にインストールしたHomebrewパッケージとバージョンを表示"
run = "brew leaves | xargs brew list --versions"

# Brewfileとの照合
[tasks.brew-check]
alias = "bc"
description = "Brewfileで管理されていないパッケージを検出"
run = '''
bash -c '
brewfile_packages=$(grep "^brew \"" ~/dotfiles/setup/Brewfile | sed "s/^brew \"\([^\"]*\)\".*/\1/" | sort)
installed_packages=$(brew leaves | sort)
unmanaged=$(comm -13 <(echo "$brewfile_packages") <(echo "$installed_packages"))

if [ -z "$unmanaged" ]; then
  echo "✅ すべてのパッケージがBrewfileで管理されています"
else
  echo "⚠️  Brewfileで管理されていないパッケージ:"
  echo ""
  echo "$unmanaged" | while read -r pkg; do
    echo "  - $pkg"
  done
  echo ""
  echo "💡 これらをBrewfileに追加することを検討してください"
fi
'
'''

# batでファイルプレビュー
[tasks.util-bat]
description = "fzfでファイルを選択してbatで表示"
run = '''
file=$(find . -maxdepth 1 -type f | sort | fzf --preview "bat --color=always {}")
if [ -n "$file" ]; then
    bat "$file"
fi
'''

# hexylでバイナリプレビュー
[tasks.util-hexyl]
description = "fzfでファイルを選択してhexylで表示"
run = '''
file=$(find . -maxdepth 1 -type f | sort | fzf --preview "hexyl --length 256 {}")
if [ -n "$file" ]; then
    hexyl "$file"
fi
'''

# Xcodeクリーンアップ（対話型）
[tasks.util-clean-xcode]
description = "Xcodeのキャッシュをクリーンアップ（対話型、clean-my-xcode）"
run = 'curl -fsSL https://raw.githubusercontent.com/filsv/clean-my-xcode/main/clean-my-xcode.sh | bash'

# Xcodeクリーンアップ（シンプル版）
[tasks.util-clean-xcode-simple]
description = "Xcodeのキャッシュをクリーンアップ（シンプル版、xcode-clean.sh）"
run = 'curl -fsSL https://raw.githubusercontent.com/niklasberglund/xcode-clean.sh/master/xcode-clean.sh | bash -s -- -A'

[tools]
# グローバルにインストールするツールを定義
# 例:
# node = "lts"           # Node.js LTS版
# python = "3.12"        # Python 3.12
# ruby = "3.3"           # Ruby 3.3
# go = "1.22"            # Go 1.22
# rust = "stable"        # Rust安定版

# === JavaScript ランタイム ===
node = ["24", "lts", "22", "20", "18"]
deno = "latest"
bun = "latest"
# npm = "latest"

# === ビルドツール ===
"npm:node-gyp" = "latest"

# === AI/LLM ツール ===
"npm:@antiwork/shortest" = "latest"   # https://github.com/antiwork/shortest
"npm:@github/copilot" = "latest"
"npm:@google/gemini-cli" = "latest"
"npm:@mixedbread/mgrep" = "latest"    # Semantic search tool for Claude Code
"npm:@openai/codex" = "latest"
# "npm:@byterover/cipher" = "latest"    # https://github.com/campfirein/cipher
"npm:@endorhq/rover" = "latest"       # https://github.com/endorhq/rover
# "npm:@letta-ai/letta-code" = "latest" # https://github.com/letta-ai/letta-code
"npm:agent-browser" = "latest"        # https://github.com/vercel-labs/agent-browser  # AIエージェント向けブラウザ自動化CLI
yt-dlp = { version = "latest", rename_exe = "yt-dlp" }  # https://github.com/yt-dlp/yt-dlp  # 動画ダウンローダー
"npm:@playwright/mcp" = "latest"      # https://github.com/microsoft/playwright-mcp/
"npm:@playwright/cli" = "latest"      # https://github.com/microsoft/playwright-cli
"npm:playwright" = "latest"            # Playwright core library (for Node.js headless scripts)
"npm:@sasazame/ccresume" = "latest"   # https://zenn.dev/sasazame/articles/3634040f7fb4da
"npm:cc-sdd" = "latest"               # https://github.com/gotalab/cc-sdd/blob/main/tools/cc-sdd/README_ja.md
"npm:cc-wrapped" = "latest"           # https://github.com/numman-ali/cc-wrapped
"npm:ccexp" = "latest"                # https://zenn.dev/nyatinte/articles/launch-ccexp
"npm:ccmanager" = "latest"            # https://zenn.dev/kbwok/articles/33fad69555d005
"npm:ccsearch" = "latest"             # https://zenn.dev/suthio/articles/126bc21f0741a6
"npm:ccusage" = "latest"              # https://zenn.dev/ryoppippi/articles/6c9a8fe6629cd6
"npm:claude-code-monitor" = "latest"  # https://github.com/onikan27/claude-code-monitor
"npm:codemass" = "latest"             # https://x.com/kenn/status/1953281949566706030
"npm:mmcp" = "latest"                 # https://zenn.dev/kou_pg_0131/articles/mmcp-introduction
"npm:ui-skills" = "latest"            # https://www.ui-skills.com/
"npm:vibe-kanban" = "latest"          # https://www.vibekanban.com/

# === 開発ツール ===
"npm:caniuse-cmd" = "latest"          # ブラウザ互換性チェック
"npm:ctx7" = "latest"                 # https://context7.com/?tab=skills
"npm:czg" = "latest"                  # コミットメッセージ支援（git-cz の後継）
"npm:difit" = "latest"                # https://zenn.dev/yoshiko/articles/difit-from-reviewit
"npm:fixpack" = "latest"              # package.json整理
"npm:git-cz" = "latest"               # Git commit支援
"npm:git-diff-archive" = "latest"     # Git差分アーカイブ
"npm:lighthouse" = "latest"           # パフォーマンス測定
"npm:unlighthouse" = "latest"         # サイト全体のLighthouseスキャン
"npm:markdownlint-cli" = "latest"     # Markdown リンター
"npm:npm-check" = "latest"            # npm依存関係チェック
"npm:npm-check-updates" = "latest"    # npm更新チェック
"npm:oxfmt" = "latest"                # Formatter
"npm:oxlint" = "latest"               # Linter
"npm:vercel" = "latest"               # Vercel CLI

# === 画像処理 ===
"npm:imageoptim-cli" = "latest"       # 画像最適化

# === Rust ===
rust = "stable"

# --- ファイル操作 ---
"cargo:filetree" = "latest"     # ファイルツリーエクスプローラー（TUI）

# --- Git関連 ---
"cargo:keifu" = "latest"        # Git commit graphs
"cargo:needle-cli" = "latest"   # GitHub PRs レビュー支援
"cargo:https://github.com/anatawa12/git-vrc" = "latest"

# --- 開発ツール ---
"cargo:cargo-update" = "latest" # cargo パッケージ更新管理
"cargo:hx" = "latest"           # Hex Viewer

# --- ネットワーク/HTTP ---
"cargo:ruget" = "latest"        # すごいwget

# --- システムモニタ ---
"cargo:rtop" = "latest"         # システムモニタリング

# === Go ===
# ※ Go製CLIツールの多くは Nix (nix/hosts/common.nix) で管理
go = "latest"  # Go本体
"go:github.com/d-kuro/gwq/cmd/gwq" = "latest"  # Git worktree管理
